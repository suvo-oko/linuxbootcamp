# Create a Linux VM with the AWS CLI

## Installing and configuring the AWS CLI

To install the AWS CLI in a Ubuntu machine:

`sudo apt install awscli`

To verify if the installation succeded:

`aws --version`

